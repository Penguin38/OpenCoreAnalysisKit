name: cmake on macos platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Debug

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Build
      run: |
        bash ./script/build_macos.sh
        tar -czvf core-parser_26.6.8.tar.gz output/aosp/debug/macos/bin/core-parser

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: core-parser_26.6.8_for_macos.tar.gz
        path: core-parser_26.6.8.tar.gz
